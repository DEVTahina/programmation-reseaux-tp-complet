# TP3 – Tests

## TCP Broadcast
1. `javac TCPServer.java` puis `java TCPServer`
2. Dans deux terminaux clients : `java TCPClient` (chaque client envoie des messages)
3. Tous les clients reçoivent les messages de tous.

## Départ d’un client : géré par le serveur (suppression de la liste).

## UDP
- `java UDPServer`
- `java UDPClient` (tapez du texte, le serveur l’affiche).